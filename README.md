# track_faster live

## How to run?
First, this is a Dockerized project. Once the project is cloned. The next step is to build 
the image by running ```sudo docker compose build``` then, run ```sudo docker compose up -d```
to run the project on development.

